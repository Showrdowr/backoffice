# Grading Feature - Frontend

ฟีเจอร์สำหรับตรวจข้อสอบแบบอัตนัย (Free Text) ใน Back Office

## 📂 โครงสร้างไฟล์

```
features/grading/
├── types.ts                          # TypeScript interfaces
├── services/
│   └── gradingService.ts            # Mock API service
└── components/
    ├── StudentInfoCard.tsx          # แสดงข้อมูลผู้เรียน
    ├── ExamInfoCard.tsx             # แสดงข้อมูลข้อสอบ
    ├── AnswerReviewCard.tsx         # แสดงคำตอบและให้คะแนน
    ├── ScoreSummaryCard.tsx         # สรุปคะแนน
    ├── GradingQueue.tsx             # Table รายการรอตรวจ
    ├── GradingForm.tsx              # Form หลักสำหรับตรวจ
    └── index.ts                     # Barrel export
```

## 🎯 การใช้งาน

### หน้ารายการรอตรวจ

URL: `/grading`

แสดงรายการข้อสอบที่รอการตรวจทั้งหมด พร้อมข้อมูล:

- ผู้เรียน (ชื่อ, อีเมล, เลขใบอนุญาต)
- คอร์สและข้อสอบ
- วันที่ส่ง
- จำนวนข้อรอตรวจ

### หน้าตรวจข้อสอบ

URL: `/grading/[attemptId]`

ตรวจและให้คะแนนคำตอบแบบอัตนัย พร้อม:

- แสดงข้อมูลผู้เรียนและข้อสอบ
- แยกแสดงข้อ Multiple Choice (read-only) และ Free Text (ให้คะแนนได้)
- คำนวณคะแนนรวม real-time
- สรุปสถานะ ผ่าน/ไม่ผ่าน
- Validation: ต้องให้คะแนนครบทุกข้อ Free Text

## 🔌 Mock Data

ปัจจุบันใช้ Mock Data จาก `gradingService.ts`

### ข้อมูล Mock ที่มี:

- **3 รายการรอตรวจ** (PendingAttempts)
- **1 ตัวอย่างข้อสอบ** (GradingDetail) ประกอบด้วย:
  - 2 ข้อ Multiple Choice (ตรวจแล้ว)
  - 2 ข้อ Free Text (รอตรวจ)

## 🔄 การแทนที่ด้วย Real API

เมื่อ Backend พร้อม แก้ไขที่ `gradingService.ts`:

```typescript
// เปลี่ยนจาก Mock
export async function getPendingAttempts() {
  // return MOCK_DATA
}

// เป็น Real API
import { apiClient } from "@/services/api/client";

export async function getPendingAttempts(params) {
  const response = await apiClient.get("/admin/grading/pending", { params });
  return response.data;
}
```

## ✨ Features

- ✅ Responsive Design
- ✅ Loading States
- ✅ Error Handling
- ✅ Form Validation
- ✅ Confirm Dialog
- ✅ Real-time Score Calculation
- ✅ Pass/Fail Indicator

## 🧪 การทดสอบ

1. เปิด `/grading` → ต้องเห็นรายการ 3 รายการ
2. คลิก "ตรวจให้คะแนน" → เข้าหน้าตรวจ
3. ให้คะแนนข้อ Free Text → คะแนนรวมต้อง update
4. กดบันทึก → ต้องมี confirm dialog
5. บันทึกสำเร็จ → กลับหน้ารายการ

## 📝 หมายเหตุ

- ทุก Component ใช้ TypeScript strict typing
- ใช้ Tailwind CSS สำหรับ styling
- รองรับ Next.js App Router
- Mock Service มี delay 500-1000ms เพื่อจำลอง API call
